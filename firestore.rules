rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Server-only collection access. Admin SDK access is governed by IAM.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
